#include<iostream>
#include<map>

using namespace std;

int main()
{
    vector<pair<int,int> > p;
    vector<int> value;
    int n;
    cin>>n;
    while(n--)
    {
        int ix, maxi;
        double real ;
        cin>>ix;
        cin>>maxi;

        for( int b = 1 ; b <= maxi ; ++b)
        {
            for(int a=0; a <= b; a++
        }
    }
}
